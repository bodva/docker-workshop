version: '2'
services:
    web:
        build:
            context: ./
            dockerfile: Dockerfile
        volumes:
            - /var/www/example:/var/www
        ports:
            - "8080:80"